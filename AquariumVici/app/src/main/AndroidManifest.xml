<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mogilevskiydenis.aquariumvici">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SaltwaterSecondMenu"
            android:label="@string/freshwater_category">
        </activity>
        <activity android:name=".SecondMenu"
            android:label="@string/fishes">
        </activity>
        <activity android:name=".calcMenu"
            android:label="Калькуляторы">
        </activity>
        <activity android:name=".Volume"
            android:label="Расчет объема аквариума">
        </activity>
    </application>

</manifest>